# Empire Lords Dragon — PRD

## Problema originale (utente)
Nuovo progetto da zero: **Empire Lords Dragon**, gioco mobile Android persistente, multiplayer, server-authoritative.
- Frontend: React Native + TypeScript + Expo. Backend: FastAPI + MongoDB persistente.
- Il JSON `/app/spec/02_CANONICAL_SPEC.json` (v3.7) è l'**unica autorità runtime** (costi, cap, timer, unlock, combat stats). Documenti di supporto: `01_BIBLE.md`, `03_TABELLE.md`, `04_RICERCHE.md`.
- Vertical slice giocabile: mappa 3D isometrica di alta qualità (chunk/streaming, LOD, pan, zoom), economia (risorse, magazzino, 21 edifici, code), ricerca (114 nodi, 12 rami), esercito e combattimento (13 unità, marce, pathfinding, target neutrali), autenticazione bilingue IT/EN con JWT + Google Auth (Emergent), UI: mappa, insediamento, code, report battaglie.
- Lingua utente: **italiano**.

## Scelte utente
- Procedere con P0 (E2E + PRD) → poi passare subito alla mappa 3D (chunk/LOD/asset) senza attendere test manuale.
- Nessuna preferenza visiva esplicita: design da `/app/design_guidelines.json` (dark slate + glass HUD + oro antico).

## Architettura
```
backend/
  server.py                     FastAPI entrypoint, lifespan (spec load, DAG validate, indexes, scheduler worker, default world)
  app/core     config, db (motor), auth (JWT + Emergent session), errors (ApiError {code,message,details}), clock (offset iniettabile), spec (loader + hash)
  app/domain   economy, construction, research, recruitment, navy, marches, combat, conquest, pathfinding (A*), scheduler (job idempotenti), sentinels, settlements, territory, worldgen, worlds, notifications, formulas
  app/api      routes_auth (/api/auth/*), routes_game (/api/worlds/*, /api/spec/*, /api/health), routes_qa (/api/qa/* admin-key + env gate)
  tests/       pytest: test_spec_formulas, test_runtime, smoke, test_public_e2e (20 test E2E via URL pubblico)
frontend/
  app/  index, login, worlds, (tabs)/{map,settlement,army,inbox}, research, queues, marches, sentinels, building/[name], target/[id], march/new, battle/[id], auth/callback
  src/  api (client + hooks React Query), components (ui, overlay, Crest, MarchCard), i18n (IT/EN), map3d (engine.ts three.js + expo-gl; terrain.ts, terrainMaterial.ts, flora.ts, castle.ts (skin), entities.ts, territory.ts, water.ts, geo.ts, MapView.tsx gesture, MapLabels.tsx, MiniMap.tsx), state (AuthContext, useGame), theme.ts
```

### Collezioni MongoDB
`accounts`, `sessions`, `worlds`, `players`, `settlements` (PLAYER / NEUTRAL / PLAYER_SLOT), `map_chunks` (terrain 32×32 bytes), `territory_tiles`, `jobs` (BUILD/RESEARCH/RECRUIT/SHIPS/SENTINEL), `marches`, `battles`, `inbox`, `sentinels`, `idempotency`.

### Endpoint principali
- Auth: `POST /api/auth/register|login|refresh|logout|session`, `GET /api/auth/me`
- Mondi: `GET /api/worlds`, `POST /api/worlds/{w}/join`, `GET /api/worlds/{w}/me`
- Insediamento: `GET .../settlements/{s}`, `.../buildings`, `POST .../buildings/{name}/upgrade`, `POST .../upgrade`, `GET .../jobs`, `POST /api/worlds/{w}/jobs/{j}/cancel`
- Ricerca: `GET .../research`, `POST .../research/{key}/start`
- Esercito: `GET .../army`, `POST .../recruit`, `POST .../ships`, `GET/POST .../sentinels`
- Mappa: `GET /api/worlds/{w}/map/chunk/{cx}/{cy}`, `GET .../map/marches?chunks=`
- Marce: `POST .../marches/preview`, `POST .../marches`, `GET .../marches`, `GET .../marches/{id}`, `POST .../marches/{id}/recall`
- Battaglie/Inbox: `GET .../battles`, `GET .../battles/{id}`, `GET .../inbox`, `POST .../inbox/{id}/read`, `POST .../inbox/read-all`
- QA (X-Admin-Key): `GET /api/qa/clock`, `POST /api/qa/clock/advance`, `POST /api/qa/scheduler/run`, `POST /api/qa/grant`

## Stato implementazione
### Completato (vertical slice) — testato E2E 10/06/2026 (iteration_1: 20/20 backend, frontend tutti i flussi OK)
- [x] Backend server-authoritative completo, scheduler idempotente, spec-driven
- [x] Auth JWT (register/login/refresh/logout) + Google Auth Emergent (exchange session_id)
- [x] Mondo generato proceduralmente (400×400, 169 chunk, 800 neutrali, 95 slot giocatore)
- [x] Economia, 21 edifici, upgrade insediamento, code, cancel
- [x] Ricerca 114 nodi / 12 rami con gating prerequisiti/università
- [x] 13 unità, reclutamento, navi, sentinelle
- [x] Marce (preview/launch/recall), pathfinding A*, combattimento vs neutrali, report battaglia, inbox
- [x] Frontend: login IT/EN, mondi/join, mappa 3D (three.js + expo-gl — vedi sezione dedicata), città, esercito, inbox, ricerca, code, marce, dettaglio target, report battaglia
- [x] Migrazione `pointerEvents` prop → style (warning RN-web)

### Mappa 3D "alta qualità" — completato e testato (iteration_2: 22/22 backend, frontend OK)
- [x] `GET /api/worlds/{w}/map/overview`: terreno 100×100 (1 cella = 4×4 tile, cache in memoria) + tutti gli insediamenti PLAYER
- [x] Terreno LOD geometrico: step 1 (vicino, <34 tile), step 2 (medio); oltre → overview mondiale sempre residente, nascosta tile-per-tile sotto i chunk caricati (`src/map3d/terrain.ts`)
- [x] Acqua: un unico piano mondiale con shader (onde vertex + value-noise shimmer, fog e colorspace corretti) — le coste emergono dal dislivello del terreno (`water.ts`)
- [x] Entità instanced per chunk (castelli con mura/torri/tetto/bandiera, tende slot, sentinelle, pin giocatori a zoom lontano) ≈10 draw call/chunk (`entities.ts`)
- [x] Colorazione per pendenza (roccia/neve), sabbia costiera, chiazze prato; cespugli su pianura e massi su montagna (LOD0); griglia tattica a zoom < 22
- [x] Etichette RN proiettate dal 3D (nome + livello, colore fazione) con toggle `map-labels-button`
- [x] Camera: inerzia pan, pinch/double-tap con punto focale, centerOn animato, pulsazione selezione/beacon casa
- [x] Google Auth hardening: session_id letto da URL di lancio (hash/query), listener deep link Android (`dismiss` ≠ annullato), guardia anti-doppio exchange, gate root (login → /worlds via stato auth), scheme app `empirelords`

### Bug fix
- [x] Mappa nera dopo Città → costruzione → indietro → Mappa (canvas collassato a 0×0 dietro il modal, viewport stantio): engine risincronizza renderer/camera col drawing buffer a ogni frame (`syncDrawingBuffer`), verificato dal testing agent (iteration_3)

### Marce sulla mappa (Bibbia §13, §41.3, deep-link map/march) — testato iteration_4/5
- [x] Marker marcia toccabile (picking in screen-space 30px, anche castelli/sentinelle) → scheda: missione → target, truppe, stato, ETA live, "Marce attive", "Richiama" (solo OUTBOUND proprie), "Battaglia" se esiste
- [x] Etichetta marcia (icona, missione/"In ritorno", countdown) proiettata dal 3D; marce in ritorno con banner pallido
- [x] LOD: marce nascoste a zoom lontano (dist > 100) come da §41.3; etichette marce entro dist 60
- [x] Richiamo = inversione reale dal punto raggiunto (DTO `recalled_at`; `engine.marchProgress`)
- [x] Marce nemiche con disclosure intel (§34.10): `incoming[]` solo dopo la detection (ingresso nel territorio), marker rosso con alone pulsante, chip "HOSTILE"/famiglia missione, scheda intel (direzione, ingresso, tipo, truppe, ETA per tier) — testato iteration_6 (backend fixture PvP + verifica visiva)
- [x] Minimappa tattica (secondo pass ortografico: terreno, punti giocatori, linee marce, footprint camera; tap → centra) — iteration_6
- [x] Stemmi Casata: crest deterministico dal nome, editor `/house` (simbolo/colori/bordo/motto, validazione catalogo), denormalizzato su insediamenti e marce; reso in 3D (bandiera sul mastio, banner marce) e in 2D (SVG `Crest.tsx`) — iteration_6
- [x] Report battaglia dalla mappa (scheda marcia → "Battaglia"; scheda insediamento → ultima battaglia) — iteration_6

### Grafica v3 "curata" (richiesta utente: niente quadrati, castello grande e decorato, skin future) — iteration_6 (0 errori shader, regressioni OK)
- [x] Terreno liscio indicizzato: vertici condivisi sugli angoli tile, rilievo fbm continuo, colori sfumati per vertice (niente scacchiera), normali analitiche (nessuna cucitura tra chunk), coste = superficie acqua che taglia il pendio (`terrain.ts`)
- [x] Shader terreno custom: grana micro (svanisce con la distanza), chiazze prato, roccia sui pendii, neve sulle creste con bordo organico, sole + emisfero, fog color "orizzonte" (`terrainMaterial.ts`, token `skyHorizon`)
- [x] Montagne a creste (ridged noise, stesso rilievo a tutti i LOD via `noiseScale`), massi ridotti
- [x] Flora: conifere a 3 livelli, latifoglie (con rari alberi autunnali), cespugli, massi; variazione colore per istanza; alberi sparsi al LOD medio (`flora.ts`)
- [x] Territorio: riempimento tenue + nastro di confine (niente tile quadrate) (`territory.ts`); griglia tattica solo a zoom < 13
- [x] Castello v2 (`castle.ts` + `entities.ts`): footprint ≈2×, basamento, mura ottagonali con merli, mastio con finestre, 4/8 torri per livello, portone con porta e torce, banner sulle mura (L≥10), torrette (L≥20), bandiera con stemma. **Skin data-driven**: `CASTLE_SKINS` classic/royal/obsidian/sandstone (neutrali = ruin); il DTO pubblico espone `skin` (null → classic) — la UI di scelta skin è un passo successivo
- [x] Marce: marker "squadra di soldati" + banner stemma; chip etichette clampate ai bordi schermo; ETA sconosciuta → "?"

### Skin, cronologia e mappa viva — iteration_7 (testing agent: 39/39 backend, frontend OK)
- [x] Skin castello per insediamento: `GET/PUT /worlds/{w}/settlements/{s}/skins|skin` (server-authoritative, sblocco per livello: classic 1 · sandstone 5 · royal 10 · obsidian 20; 400 INVALID_SKIN / 409 SKIN_LOCKED)
- [x] Schermata `/skins` (pulsante tavolozza in Città): anteprima 3D live (`CastlePreview` GLView, stesso motore della mappa, orbita lenta, fumo), card skin con lucchetto/livello richiesto/"In uso", Applica
- [x] Cronologia battaglie nella scheda insediamento sulla mappa: ultime 5 (esito, missione, data, perdite, bottino, conquista) → tap apre il report; endpoint `GET .../settlements/{s}/battles?limit=5` (target o origine, solo battaglie del visualizzatore; le nuove battaglie salvano `origin_settlement_id`)
- [x] Mappa viva: bandiere che ondeggiano (vertex shader `onBeforeCompile`, istanziate + banner marce), bagliore torce/bracieri pulsante (additivo), fumo stilizzato da camini, torce e bracieri presidiati (`SmokeSystem`, solo zoom < ×48)
- [x] Orologio QA persistito in Mongo (`qa_state.clock`) — il restart del backend non azzera più i timer

### Missioni personali, Prestigio, Achievement, Cronaca — iteration_8 (Bibbia §20/§22/§39)
- [x] Tab "Missioni" (segmenti Missioni / Casata / Cronaca), composer `/mission/new`, catalogo 5 missioni TIMED con cooldown/slot (max 2), reward = snapshot produzione × ore (cap Magazzino) + Prestigio; ledger Prestigio, achievement a tier, Cronaca del mondo; `GET/POST /worlds/{w}/missions`, `GET .../progress`, `GET .../chronicle`

### Carovane — iteration_9 (Bibbia §13 / §34.9, spec.caravans) — testing agent frontend OK + pytest `tests/test_caravans_e2e.py` 5/5
- [x] Backend: slot logistici astratti (Caravanserraglio: 1 + ceil((L-1)/5) carovane/marcia, cap 5000×1,17^(L-1) × bonus ricerca), velocità 3 tile/h (scorta = unità più lenta), max 1 carovana in uscita/insediamento (conta nelle 5), consegna con cap Magazzino: **l'eccedenza resta sul convoglio e torna al mittente** (`DELIVERED_PARTIAL` → RETURNING), ricerca carovane straniere per raggio Chebyshev (5 + bonus ricerca, max 12) con disclosure intel, intercettazione (primo waypoint raggiungibile scelto dal server, combattimento normale, loot cap cargo sopravvissuti, residuo torna al mittente), notifiche `CARAVAN_STATE`, achievement `caravans_intercepted`
- [x] Endpoint: `GET .../settlements/{s}/caravans/info`, `POST /worlds/{w}/caravans`, `GET .../settlements/{s}/caravans/search`, `POST /worlds/{w}/caravans/intercept`
- [x] Frontend: hub `/caravans` (pulsante in Città), composer `/caravan/new` (destinazione tra i propri insediamenti, slot, carico con clamp capacità, scorta opzionale), `/caravan/intercept` (solo unità ATK>0), carovane straniere rilevate come marker ostili sulla mappa 3D (`caravanAsMarch`, orologio server) con scheda + Intercetta, inbox CARAVAN_STATE/BATTLE_RESOLVED con deep link, `MarchListCard` condiviso (marce + carovane con righe carico/consegnato/eccedenza)
- Fixture QA: account rivale `rival@empirelords.com` (vedi test_credentials.md)

### Alleanze — iteration_10/11 (Bibbia §19 / §34.7 / §40) — pytest `tests/test_alliances_e2e.py` 8/8 + testing agent UI (2 pass, 4 account) OK
- [x] **Strutturate** (cap 100, Piramide, ruoli Leader/Vice/Diplomatico/Membro con permessi da spec, chat, tesoreria Smeraldi, PNA, guerre con voto 12h a maggioranza matematica, assedio condiviso) **vs Mercenarie** (cap 5, fuori Piramide, nessun voto di guerra: guerre solo come effetto dei contratti accettati) — separazione rigida (regola utente)
- [x] Membership: creazione (nome/sigla unici), elenco pubblico, inviti 72h con ruolo, accetta/rifiuta, uscita con ritardo 12h + cooldown 24h (72h se in guerra + war_involved), successione Leader (Vice più anziano → membro), scioglimento (rimborsi/esiti contratti), kick/ruoli/trasferimento
- [x] Diplomazia per coppia: PNA proponi/accetta/rifiuta/termina (preavviso 12h), voto di guerra, pace (proposta 24h, accettazione Leader/Vice → PEACE_PENDING 12h → NEUTRAL), pace bloccata durante contratto mercenario; **gate lanci ostili** (CANNOT_ATTACK_ALLY / DIPLOMACY_BLOCKS_ATTACK) su marce e intercettazioni; RINFORZO e Carovane verso alleati; tile alleati con fattore 0,90
- [x] Tesoreria Smeraldi: registro append-only, visibile a Leader/Vice; fonti: difesa PvP +10 (cooldown coppia 24h), conquista PvP +25, prima missione/giorno +5 (solo Strutturate, cap 500/giorno)
- [x] Contratti mercenari: escrow 1000–1M, durate 72/96/120/168h, max 3 attivi, accettazione → guerra automatica + lock pace, scadenza/scioglimento bersaglio = successo (escrow al fornitore, +10 prestigio mercenario alleanza e membri), scioglimento fornitore = fallimento (rimborso); bonus fornitore vs bersaglio +5% cap marcia / +3% ATK (snapshot `bonuses` sulla marcia)
- [x] Frontend: segmento **Alleanza** nel tab Missioni (dashboard / lupo solitario con inviti), `/alliance/create`, `/alliance/browse`, `/alliance/[id]` (azioni diplomatiche), `/alliance/members`, `/alliance/diplomacy` (relazioni + voti), `/alliance/chat`, `/alliance/treasury`, `/alliance/mercenary`; inbox eventi alleanza con deep link; mappa: fazione ALLEATO (colore, legenda, tag `[SIGLA]`), composer solo RINFORZO verso alleati
- Fixture QA: account ally/third (vedi test_credentials.md), `POST /qa/alliance/emeralds`

### Rubini & premium — iteration_12 (Bibbia §23, spec.premium) — pytest `tests/test_premium_e2e.py` 8/8 + testing agent UI OK
- [x] Wallet Rubini a livello account (`accounts.rubies`, registro `ruby_transactions` idempotente per chiave), `GET /wallet`, Rubini in `/me`; negozio DISABILITATO (catalogo Play Billing vuoto) → nessun acquisto; concessione QA `POST /qa/rubies`
- [x] **Completa ora** su job già avviati: costruzione max(100, min residui×3), ricerca max(150, ×4), reclutamento max(100, ×2,5); vietato per unità speciali/leggendarie (Carro, Drago/Angelo/Demone), con assedio attivo o marcia ostile in arrivo ≤60 min; debito atomico con rollback, stesso handler di completamento dello scheduler; pillola `◆N` su code Città, /queues, dettaglio edificio, ricerca, esercito
- [x] Cosmetica: **rinomina Casata** 500 Rubini (catalogo `cosmetics_v1`, nome unico nel mondo, copie denormalizzate aggiornate); Casata con **descrizione** (§20) editabile
- [x] **Specializzazione** giocatore (spec.player_specialization): Attaccante/Difensore +5%, disponibile con 3 insediamenti, prima scelta gratis, cambio 2.500 Rubini, cooldown 7g, bloccata in guerra/assedio/marce militari; il combattimento legge le chiavi spec
- [x] UX: **Alleanza è un tab** della barra inferiore (accanto a Esercito), impostazioni alleanza (nome unico + descrizione) per il Leader; schermate `/wallet`, `/specialization`
- Nota: nella Bibbia le missioni personali generano **Smeraldi** per l'alleanza (+5 prima missione/giorno per membro, solo Strutturate) — non Rubini; i Rubini sono solo da acquisto (catalogo vuoto) o QA

### Piramide endgame — iteration_13 (Bibbia §21 / §31.7 / §34.8 / §39.2, spec.pyramid) — pytest `tests/test_pyramid_e2e.py` 8/8
- [x] Backend `domain/pyramid.py`: macchina a stati per mondo (collezione `pyramid`, `_id`=world_id) DORMANT_INITIAL → OPEN → REWARD_LOCK → DORMANT → OPEN…, eventi `PYRAMID_STATE_DEADLINE` con `scheduled_at` persistiti (`deadline_key` scarta gli eventi stantii), `epoch` per chiave anti-collisione dei reset QA
- [x] **Configurazione modulare**: default da spec.pyramid + override per mondo `worlds.pyramid_config` (`GET/PUT /api/qa/pyramid/config`, `POST /api/qa/pyramid/reset {clear_config, config}`) — durate, giorno di apertura, cap presidio, % premi, Smeraldi, Prestigio, Guardiano; un cambio ricalcola la scadenza pendente dagli anchor persistiti (`_reschedule`)
- [x] Guardiano ad ogni OPEN: top-10 development_score → mediana max legal march power (cap Sala di Guerra × 11,35) → clamp 250k–1,5M → 45/35/20 Fanteria/Arciere/Cavalleria
- [x] Marce `target_pyramid` (ATTACK solo Strutturate/OPEN/non owner; REINFORCE solo owner, cap 1M): battaglia vs presidio (`defender_kind PYRAMID`), sopravvissuti = presidio (`garrison_by_player`), perdite ripartite per contributore, cambio owner azzera l'hold, partecipazione +500 Smeraldi/alleanza/ciclo + Prestigio +50 + sigillo bronzo
- [x] Hold completato → REWARD_LOCK: snapshot membri (`players.pyramid_reward` + denormalizzato sugli insediamenti), +8% produzione / +5% ricerca / +5% addestramento / +10% cap carovane (formule `extra_pct`, non nel cap ricerca), titolo "Signore della Piramide", sigillo oro, Prestigio +250, +2000 Smeraldi, Cronaca `PYRAMID_WON`; REWARD_LOCK → DORMANT: presidio torna a casa con marce reali (`PYRAMID_RELEASED`); scioglimento alleanza → neutralizzata
- [x] `GET /worlds/{w}/pyramid` (stato, countdown, owner/hold, presidio — composizione solo se neutrale o propria —, battaglie recenti dell'epoca, attacchi in arrivo per l'owner, `me.*`, config, storico); notifiche `PYRAMID_STATE_CHANGED` (deep link `pyramid`)
- [x] Frontend: monumento 3D 15×15 a (200,200) (`map3d/pyramid.ts`, aspetto per stato/fazione, braciere+pilastro di luce in OPEN), label mappa con countdown, tap → scheda con azioni, pulsante "centra sulla Piramide", schermata `/pyramid`, `march/new?pyramid=1`, tile nel tab Alleanza, inbox
- world_1 demo: override `first_open_day: 1` → Piramide OPEN con Guardiano spec (22k unità)

### Allerta Piramide + Cinematiche — iteration_14 (Bibbia §21 / §41.2)
- [x] **Allerta Piramide** (scelte utente: tutti i membri dell'Alleanza detentrice; solo tag + ETA): a ogni ATTACK lanciato contro la Piramide detenuta → `PYRAMID_ATTACK_INCOMING` (HIGH, deep link `pyramid`) in Inbox per ogni membro + riga di sistema in chat alleanza "⚠ Attacco alla Piramide in arrivo da [TAG] · ETA"; `status.incoming` = `{march_id, attacker_alliance_tag, arrival_at}` (nessuna casata/dimensione); banner rosso `pyramid-alert-banner` su Mappa (sotto HUD) e tab Alleanza + lista "Attacchi in arrivo" in `/pyramid`; marce salvano `alliance_tag`
- [x] **Cinematiche skippabili** (`src/components/cinematic/Cinematic.tsx`, `CinematicProvider` nel root layout): **PARTENZA** al lancio della marcia (composizione reale, stemma Casata, banner Alleanza, ETA) 4s — 6s "Grande offensiva" se Leggendario o ≥25.000 unità; varianti: Drago/Angelo/Demone scende dal cielo, sbatte le ali e avanza con l'esercito sputando fuoco; Falco fa passate di ricognizione. **CONQUISTA** 8s (bandiera nemica cade, stemma sale con scintille, superstiti reali; variante Piramide "La Piramide è nostra!") auto-play una volta all'apertura del rapporto (`eld.cinematic.conquest.seen`) + pulsanti "Rivedi la partenza" / "Rivedi cinematica" nel rapporto di battaglia. Tasto "Salta" dopo 1s, barra di avanzamento, non blocca i timer server
- world_1 demo: la Piramide è ora **detenuta da [DEMO]** (hold 168h) con un attacco [TRZ] in arrivo → allerta visibile a demo/ally; Guardiano riportato ai valori spec per il prossimo ciclo

### Cinematiche key-art AI — iteration_15/16 (richiesta utente: "scene tipo film epico"; il 3D procedurale è stato rimosso) — testing agent 7/7 OK
- [x] 16 immagini epiche (2 inquadrature × 8 varianti: standard, Falco, Grande offensiva, Drago, Angelo, Demone, Conquista, Piramide) generate **una volta** in sviluppo con Gemini Nano Banana (`backend/scripts/gen_cinematics.py`, Universal Key, 9:16, JPEG ≤1080 px ≈ 125 KB ciascuna) e bundlate in `frontend/assets/cinematics/`
- [x] Player `CinematicArt.tsx`: movimenti di camera Ken Burns (push-in / pull-back), taglio con dissolvenza + flash di luce al 55%, braci che salgono, barre letterbox, lieve oscillazione "handheld" per Leggendari/Grande offensiva; l'orologio parte al decode della prima immagine (fallback 1,5 s); tutto è funzione del tempo → skip-safe
- [x] HUD invariato (stemma, Casata, [TAG], titolo, composizione reale, "Salta" dopo 1 s, barra); scrim a gradiente; marker `cinematic-variant-*`
- [x] Galleria **`/cinematics`** (Casata → "Cinematiche"), righe disabilitate finché il giocatore non è idratato; mappa in pausa durante lo show (`setMapRenderHold`)

### Grafica 3D mappa "cinematografica" — iteration_16 (richiesta utente) — testing agent 7/7 OK
- [x] Renderer: ACES filmic tone mapping (exposure 1,05); **ombre del sole in tempo reale** (shadow map 2048 che segue il target camera, raggio in funzione dello zoom) ricevute dal terreno (shader custom con i chunk shadow di three) e dai castelli; casting da castelli, alberi, Piramide
- [x] **Texture procedurali tileable** (`map3d/textures.ts`, CPU, identiche su Android/web): atlas dettaglio terreno (erba/suolo, roccia a creste, grana), corsi di pietra con malta e tono per blocco, tegole. `mergeGeos` ora conserva le UV
- [x] Terreno: dettaglio a due scale + chiazze di terra secca, roccia con crepe, neve, occlusione tenue; acqua con normali d'onda, riflesso del sole (glint + gloss), Fresnel verso il cielo, creste ventose
- [x] Castelli: muratura in pietra su mura/mastio/torri/porta/plinto, tegole sui tetti (le skin tingono via instance color) — anche in `CastlePreview` (/skins)
- [x] Alberi: conifere a 4 livelli con bordi irregolari e AO cotta nei vertex color, latifoglie a 5 blob con tronco biforcuto, cespugli/massi jitterati, **vento** (vertex shader seedato per istanza)
- [x] **Piramide** rifatta (`map3d/pyramid.ts`): 7 livelli in muratura, bande dorate sui bordi, scalinata monumentale con balaustre e bracieri, 4 obelischi con pyramidion, tempio sommitale con colonne + capstone d'oro, pilastro di luce con alone rotante; stati/fazioni invariati (API `setLook/tick`), `PYRAMID_TOP` 10,6

### Intro 24 s + Notte/Giorno del Regno + Skin marce — iteration_17 (richiesta utente) — testing agent 12/12 OK
- [x] **Intro cinematica** (spec.cinematics.intro: 24 s, skip dopo 2 s, non blocca i timer): 6 illustrazioni AI (`assets/cinematics/intro_0..5.jpg`) con didascalie narrative IT/EN (regni divisi → Casate → Drago → Piramide → Alleanze → "la Casata {house} entra nel Regno"), taglio ogni 4 s. Auto-play alla **prima entrata** Player/World (`IntroGate` in `(tabs)/_layout`, flag server `players.intro_seen_at` via `POST /worlds/{w}/intro/seen`, `/me → player.intro_seen`); rivedibile da **Missioni → Cronaca → "Rivedi l'intro"** e dalla galleria `/cinematics`
- [x] **Notte e giorno del Regno** (`map3d/daylight.ts`): un solo orologio per tutti = ora server **UTC+1**; keyframe notte 0-5 → alba 6:30 → giorno 8:30-17 → tramonto 18:30 → notte 20:30; interpolazione smoothstep di sole (colore/intensità/direzione), emisfero, orizzonte/fog, esposizione, uniform terreno/acqua; notte **leggibile** (blue hour), torce/bracieri ×2.3 e finestre dei castelli illuminate. Chip HUD `map-realm-clock` "HH:MM · Alba/Giorno/Tramonto/Notte"
- [x] **Skin marce** (Casata → "Skin delle marce"): classica / Drago / Elefante / Falco, **sbloccate possedendo davvero la creatura** (≥1 unità in guarnigione o in marcia; `house.MARCH_SKINS`, `PUT /house {march_skin}` → 409 `MARCH_SKIN_LOCKED`), salvataggio immediato, aggiornamento delle marce in volo; snapshot `skin` sulle nuove marce (DTO). Sulla mappa il marker mostra il rig low-poly (`map3d/markerSkins.ts`): Drago in volo sopra la colonna con battito d'ali e bagliore di fuoco, Elefante da Guerra in testa con howdah nel colore di fazione, Falco che volteggia
- [x] Ombre: shadow map ridotta a 1024 (performance su GPU deboli / preview software)

### Chat del Regno, Login giornaliero, Mercenari diretti, look "adulto" — iteration 18 (richiesta utente)
- [x] **Chat Dock** persistente: barra sottile sopra i tab (`ChatDock` iniettato nel `tabBar` di `(tabs)/_layout` via `expo-router/build/react-navigation/bottom-tabs` → `BottomTabBar`; su iOS 26 NativeTabs → dock flottante) con anteprima dell'ultimo messaggio e pallino non-letti; pannello full-screen con canali **Regno** (rate limit 3 s), **Alleanza**, **Trattative** (stanze private tra due alleanze, ruoli Leader/Vice/Diplomatico) — backend `domain/chat.py`, `GET /chat/summary|world|negotiations/{id}`, `POST /chat/world|negotiations/{id}`; `openChat()` deep-open da altre schermate
- [x] **Login giornaliero** (`domain/daily.py`, `/daily`, `/daily/claim`): ciclo 7 giorni **solo risorse** (scelta utente, iterazione 19: ×1 / ×1,25 / ×1,5 / ×1,75 / ×2 / ×2,5 / giorno 7 forziere ×4 — base 8 % del cap Magazzino della capitale; niente accelerazioni), giorno del Regno UTC+1, serie azzerata se si salta un giorno, claim atomico. Schermata **`/daily`**: key-art del caveau (`vault_0.jpg`), **forziere 3D** (`ChestGL`, expo-gl + three, canvas trasparente a schermo intero con view-offset per posare il forziere sul piedistallo dipinto; coperchio, bagliore, fontana di monete e scintille al reclamo), striscia 7 giorni, card "Hai ricevuto"; auto-apertura una volta per sessione se reclamabile (`DailyGate`, dopo l'intro), pulsanti regalo con pallino su Mappa (HUD) e Città.
- [x] **Elenco Mercenari + contratti diretti** (`/alliance/mercenary`): alleanze mercenarie con prestigio/contratti/slot/disponibilità, "Proponi contratto" → offerta **diretta** (`provider_alliance_id`, solo loro possono accettarla), "Tratta in chat" apre la stanza di trattativa; `GET /worlds/{w}/mercenaries`
- [x] **Pulsante Carovana** nella scheda insediamento sulla mappa (propri insediamenti e alleati) → `/caravan/new?target=`
- [x] **Look "adulto"** della mappa (stile Empire Four Kingdoms): palette terreno più scura e desaturata (`theme.ts`), grading nel shader terreno (desaturazione 24 %, gamma 1,12, cast terroso, ombre più profonde), luce diurna più fredda e meno intensa (`daylight.ts`)
- [x] **Anteprima 3D skin marce** in Casata (`MarchSkinPreview`: colonna + stendardo con stemma + creatura, orbita; tap su skin bloccata = solo anteprima) · **Key-art missioni** (5 banner 16:9 generati con Nano Banana, `mission_*.jpg`) sulle card Missioni e nel composer
- [x] **Account Max + rapporto di fattibilità** (`backend/scripts/max_account.py` → `max@empirelords.com / Max12345!`, tutto al cap; `/app/memory/MAX_ACCOUNT_REPORT.md`: costi/tempi per raggiungere ogni cap, verifica cap Magazzino su ogni passo della progressione → **nessun passo irraggiungibile**; stima ≈ 20 mesi senza Rubini + 126 giorni Leggendari)
- [x] **Niente codici grezzi nella UI** (richiesta utente): Cronaca (eventi Piramide in frasi), Storia della Casata (motivi prestigio, vittorie Piramide, rinomine), Esercito/Città/Edificio (categorie tradotte, riga sblocco «Richiede: Insediamento liv. N · ricerca «Nome» · Edificio» con `required_research_name` dal backend), Inbox (titoli evento, stati code, nome ricerca, esiti marce/contratti, motivi tesoreria, messaggi di trattativa; nessun JSON), Tesoreria, Rapporto battaglia

### Prossimi (P2)
- [x] Eliminazione per inattività (3 gg nei primi 30 gg del Regno → slot riaperto; poi 120 gg → neutrale) e cap Leggendari — iteration 31
- [x] Skin marce come premio Prestigio (Falco 500 / Elefante 2000 / Drago 5000, oppure possesso creatura) — iteration 33
- [ ] Verifica Google Auth con flusso reale su dispositivo (Expo Go / build)
- [ ] Test scheduler su restart backend (eventi persistenti con lease: design ok, verifica pratica)

### Backlog (P3)
- [x] Santuario Mitico + Unicorno / Ponte Arcobaleno (§12) — iteration 32
- [x] Cinematica Arcobaleno (ponte Unicorno / conquista) — iteration 33
- [ ] Cinematica Intro (24s, prima entrata) rivedibile dalla Cronaca (§41.2)
- [x] Negozio: pacchetti Rubini (Google Play/RevenueCat) + skin castello premium — iteration 34
- [ ] Collegamento Google Play reale (chiave RevenueCat, prodotti, webhook, build Android) — in attesa account Google Play
- [ ] Metadata Google Play / Android App Bundle

## Credenziali test
Vedi `/app/memory/test_credentials.md` (demo@empirelords.com / Demo12345!, admin key in backend/.env).

## Note operative
- Non modificare `/app/spec/*` né `backend/.env` (SPEC_EXPECTED_HASH verificato all'avvio).
- Timer canonici lunghi: usare `/api/qa/clock/advance` in test.
- Mappa 3D: tutta la logica resta in three.js + expo-gl (compatibile Android nativo; su web usa WebGL via expo-gl).
- pytest: `cd backend && pytest tests/test_public_e2e.py -o addopts=''` (xdist rompe le fixture di sessione).

### Villaggio 3D vivo, Profilo/Esci, filtri Inbox — iteration 19 (richiesta utente)
- [x] **Villaggio 3D nella Città** (`src/city/village.ts`, `villagers.ts`, `CityScene.tsx`, `useVillageInput.ts`; route `/city` a schermo intero): castello (stessi rig della mappa) al centro, anello civico con i 13 edifici canonici modellati uno per uno (Magazzino, Caserma con manichini, Università con cupola, Tempio, Scuderia con cavalli, Officina con catapulta, Bestiario con gabbie, Santuario con cristallo fluttuante…), anello risorse (Fattoria con campi/pagliai/mulino a vento animato a L20+, Boscaiolo, Cava con fornace fumante, Miniere con carrello, Porto con molo e barca), case di riempimento che aumentano col livello (2 → 26), mercato al cancello da L5, **Mura** ad anello con altezza/torri (4/8/12) legate al livello Mura e porta fortificata, alberi e rocce fuori le mura. Tre tier di crescita per edificio (L1-9, L10-19, L20-30), lotti liberi con cartello per gli edifici sbloccati ma non costruiti. **Abitanti animati** (3 → 30: contadini, mercanti, guardie con torce che pattugliano le mura, nobili) che camminano tra porte/anello/mercato/cancello, carri con buoi sull'anello da L8. Luce del Regno giorno/notte (finestre e torce si accendono), fumo, ombre. Geometria statica unita per materiale (~15 draw call). Gesti: trascina = ruota, pizzica = zoom, tocca = seleziona edificio (nel pannello apre subito la scheda edificio; a schermo intero mostra la card con «Apri edificio»/«Costruisci»). Il rendering si ferma quando la schermata non è a fuoco e mette in pausa la mappa.
- [x] **Profilo e impostazioni** (`/settings`, ingranaggio nell'header Città): account, mondo, lingua IT/EN, Cambia mondo, Casata, **Esci** (conferma su mobile) — prima il logout era raggiungibile solo dalla schermata mondi.
- [x] **Login giornaliero solo risorse** (rimossi speed-up, endpoint `/jobs/{id}/speedup` e pillola ⏩).
- [x] **Filtri Inbox**: chip Tutte / Battaglie / Code / Marce e carovane / Alleanza / Regno con conteggio non letti per categoria (inbox caricata a 200 voci).
- [x] **Regola Bibbia «nessun edificio sopra il livello dell'insediamento»** resa coerente anche nei dati QA: `qa/grant` alza l'insediamento (e il Castello) al livello dell'edificio più alto concesso; `scripts/repair_building_levels.py` ha sistemato i dati esistenti (Casa Demo L3 → L10 perché Università/Sala di Guerra erano L10). La costruzione già rifiutava upgrade oltre il livello (`BUILDING_CAP_SETTLEMENT_LEVEL`).
- [x] **Selettore truppe** (missioni + marce) ridisegnato come card a due righe (`UnitStepper`): icona unità, nome, disponibili, valore; stepper largo −/campo/+ con scorciatoie ½ e MAX e barra di riempimento — niente più testo in verticale sui telefoni stretti.

### Grafica "storybook" stile Game of Warriors — iteration 20 (richiesta utente: niente look cupo, niente "mattoncini quadrati", 3D moderno)
- [x] **Città 3D**: ogni blocco è un `RoundedBoxGeometry` (spigoli smussati, i listelli sottili restano box), tetti a due falde "gonfi" (`gable` con rigonfiamento, tavole di gronda e colmo arrotondato), tetti conici a "cappello di strega" curvo (`hat()` in `castle.ts`, condiviso con la mappa), alberi a chiome grumose (`blob` = icosaedro jitterato), pini a livelli svasati, massi organici; **prato ondulato** (`meadow()`: disco a rilievo piatto dentro le mura, colline morbide fuori, bordo che sale a nascondere l'orizzonte, colori per vertice con chiazze di sole e strada consumata, il viale del cancello resta in piano); **cielo** (`src/city/sky.ts`: cupola a gradiente zenit→orizzonte con alone del sole e stelle di notte + 10 nuvole soffici low-poly che ruotano lentamente), luce diurna calda (sole 2.05, emisfero azzurro/verde 1.0), ombre PCFSoft, fog solo sulle colline lontane. Palette satura (erba `#68bb42`, tetti teal/blu/rosso/viola, intonaco caldo, strade sabbiose); **rimosso il contorno a inchiostro** (inverted hull) — look pittorico, non fumetto. Camera: pitch 0.26–1.25 (si può inclinare fino a vedere cielo e nuvole), default più basso.
- [x] **Pietra "naturale"**: `textures.makeStoneTexture` ora è conci grandi irregolari (4 corsi, larghezze jitterate, giunti chiari a basso contrasto, bevel morbido) — niente più griglia di mattoncini su castelli/Piramide; `cityTextures.stone` = pietra a secco Voronoi; ciottolato con giunti sabbiosi; tegole a contrasto ridotto. Materiali castello Lambert lisci (`flatShading: false`), boost pietra 1.28 / tegole 1.6.
- [x] **Castello (mappa + città)**: mastio e corpo di guardia in `RoundedBoxGeometry`, torri a 12 segmenti, merli smussati, tetti curvi (`hat`), pali angolari cilindrici — stessi ancoraggi (camino, torce, bandiera) → nessun impatto su fumo/etichette.
- [x] **Mappa del mondo**: token `theme.ts` vividi (`terrainPlain #5DAB3C`, `terrainForest #2E8A48`, `terrainMountain #8E877C`, `terrainWater #2F8ED8`, `skyHorizon #A9D6F5`), shader terreno senza grading "adulto" (ora +14 % saturazione e ombre leggermente sollevate), preset `daylight.ts` più caldi e luminosi (giorno: sole `#fff3dc` 2.05, cielo `#bfe1ff`, esposizione 1.08; notte blu leggibile; alba/tramonto arancio), moltiplicatori terreno 0.95/0.56. Ciclo giorno/notte mantenuto (scelta utente).
- Regole di gioco/Bibbia **invariate** (solo grafica).

### Sentinelle, territorio e Regni più grandi — iteration 21
- [x] **Settori a spicchio** (`sentinels.sector_tiles`): Sentinella interna (raggio 3) = spicchio di 90° del quadrato 7×7 intorno al castello (12 tile ciascuna, una diagonale a testa in senso orario N+NE, E+SE, S+SW, W+NW → con tutte e 4 il quadrato pieno, 48 tile + BASE); esterna (raggio 5, Perimetro Avanzato) = spicchio di 45° della fascia 4–5 (8–10 tile; con tutte e 8 → 11×11). Acqua e tile altrui escluse dal claim (single-owner), cade solo lo spicchio della Sentinella persa. Script `scripts/recompute_sentinel_sectors.py` ricalcola le Sentinelle esistenti; `scripts/demo_sentinel.py` costruisce+presidia una Sentinella per la Demo (fixture). Casa Demo (Regno 1) ha ora 4 Sentinelle interne GUARDED.
- [x] **Alleati uniti sulla mappa** (`territory.ts`): il nastro di confine non viene disegnato tra tile OWN e ALLY (Bibbia §14 ALLIED_SHARED_BORDER); i riempimenti restano di colore diverso; confini nemici netti (nessun annullamento: chi reclama prima tiene la tile finché la sua Sentinella non cade).
- [x] **Dimensione Regno configurabile** (`worldgen.GenConfig`): size, distanze giocatori (min/preferita), distanze neutrali (min/preferita), ancora Piramide (default centro). Il layout della massa continentale scala con S = size/400 (isole, ellissi continente, catene montuose, semi foresta, mare minimo per porti ∝ S²); output **identico** al generatore precedente per 400 (verificato). Backend size-agnostic: `pathfinding.load_terrain` legge `world.size`, territorio/marce/sentinelle usano `grid.shape`, chunk range e overview dal mondo. `POST /api/worlds` (admin) accetta gli override. Frontend: `MapEngine` prende `worldSize` (world DTO) e l'ancora Piramide dal DTO; `MapView3D` monta la scena solo quando `me.world.size` è noto.
- [x] **Regno 2** creato: 600×600, 100 slot, 800 neutrali, distanza minima tra giocatori 16 (preferita 20 → ottenuta 20), neutrali min 6 (pref. 9 → ottenuta 7), Piramide (300,300). Regno 1 invariato. Tempi/costi/conteggi Bibbia invariati.
- Notte più scura e blu (`daylight.ts` NIGHT: sole 0.75, esposizione 0.82).

### Confine Naturale + Vista Sentinelle Città — iteration 22
- [x] **12 slot Sentinella** (Bibbia §14: 4 interne N/E/S/O a r3 + 8 esterne in tutte le direzioni a r5): `sentinels.ring_for/slot_of/all_slots`; `POST .../sentinels {direction, ring?}` (cardinali → INNER di default, `ring:"OUTER"` per l'anello esterno; diagonali sempre OUTER); unicità per (insediamento, direzione, anello); target job `SENTINEL:<ring>:<dir>` (etichetta coda «Sentinelle NE · r5»). Schermata `/sentinels`: griglia interna (4) + esterna (8, bloccata senza Perimetro Avanzato), testID `sentinel-build-{dir}` / `sentinel-build-outer-{dir}`.
- [x] **Confine naturale** (`sentinels.natural_directions/sync_natural_boundaries`): se la tile dello slot è ACQUA o fuori mappa (mai montagna) la Sentinella non serve: lo spicchio di terra viene reclamato automaticamente come `NATURAL:<sid>:<ring>:<dir>` (idempotente, nessun presidio, nessuna scadenza; l'acqua non diventa terra posseduta). Gate come le torri: Comando Sentinelle ≥1 (interne), Perimetro Avanzato (esterne). Sync ad ogni `GET .../sentinels` (che ora ritorna `natural[]` + `outer_unlocked`) e su build; costruire su acqua → 409 `NATURAL_BOUNDARY`. Conquista: `release_settlement` cancella anche le NATURAL, il nuovo proprietario le riottiene al primo GET. UI: slot azzurri «confine naturale» + pannello esplicativo (`sentinels-natural-panel`). Test puri `tests/test_sentinel_geometry.py`.
- [x] **Vista Sentinelle Città** (`src/city/sentinelTowers.ts`, autonomo): intorno alle mura del villaggio 3D vengono disegnati i 12 slot: PRESIDIATA = torre con braciere acceso (fiamma che tremola, più forte di notte) e bandiera di fazione che ondeggia; IN GRAZIA = torre con braci spente, niente bandiera; IN COSTRUZIONE = torre a metà con impalcatura; VUOTO = basamento in pietra + palo con cartello; CONFINE NATURALE = stagno con canne e sassi. Interne a `wallR+1.05`, esterne a `wallR+2.9` (solo se Perimetro Avanzato o già presenti), quota dal prato. Dati: `useVillageInput` → `useSentinels` → `sentinelSlots()`. Camera Città allontanata (`wallR*2.2+2`, ×1.5 in portrait; zoom-out max `wallR*3.8+8`), shadow camera ±13.
  - **ROLLBACK rapido** (se non piace): eliminare `frontend/src/city/sentinelTowers.ts`; in `village.ts` togliere l'import `buildSentinelTowers`, il campo `sentinelSlots?` in `VillageInput`, il blocco «Sentinel towers around the walls» (3 righe) e `towers?.dispose()/towers?.tick(...)`; in `useVillageInput.ts` togliere `useSentinels`, `sentinelSlots` e il campo `sentinelSlots:`; in `CityScene.tsx` la riga `v.tick(t, r.night)` può restare (anima anche le bandiere delle mura). Backend e schermata Sentinelle NON sono coinvolti.

### Account "Lord" (fixture QA) — iteration 23
- [x] `scripts/lord_account.py` (idempotente): registra **lord@empirelords.com / Lord12345!** in Regno 2 (`world_2`, Casata «Casa Lord»), sceglie uno slot libero con tutte le 12 tile Sentinella su terra e ≥4 neutrali entro 30 tile; casa madre (491,543) a L30 con tutto al cap (stesso stato del max account) + **12 Sentinelle GUARDED** con presidio (spicchi reclamati → dominio 11×11; slot su acqua → confine naturale via `sync_natural_boundaries`); 4 villaggi conquistati con `conquest.transfer_ownership` (flusso server reale) e sviluppati a L18/L14/L10/L6 rispettando il cap edifici ≤ livello. `worlds.join_world(..., slot_id=)` accetta ora uno slot preferito (solo strumenti QA).

### Carovane con scorta, rinforzi ai propri castelli, «Carovane nei dintorni» — iteration 24
- [x] **Carovana con scorta «non parte»**: causa reale = cap Bibbia «max 1 carovana in uscita per insediamento» (l'utente aveva già una carovana in viaggio dalla casa madre) → il server rispondeva `CARAVAN_OUTGOING_MAX` mostrato come codice grezzo. Fix UX: il compositore controlla in anticipo le marce in uscita (`useMarches`) e mostra il blocco «Una carovana è già in viaggio…» / «Hai già 5 marce in uscita…» disabilitando il pulsante; avviso se la scorta è attiva ma vuota; **toast in italiano** per i codici server più comuni (`API_ERRORS` in `src/i18n`, usato da `showError` in `overlay.tsx`). La scorta lato server funzionava (verificato: carovana scortata 201 da un villaggio).
- [x] **Rinforzi ai propri castelli**: mancava il pulsante — la scheda insediamento sulla mappa per i PROPRI insediamenti (non attivo) ora ha **«Rinforza»** (`map-selection-reinforce`) → `/march/new?target=` con missione Rinforzo preselezionata (backend già OK: preview + launch REINFORCE 200).
- [x] **«Carovane nei dintorni»** (`app/caravan/nearby.tsx`): pulsante binocolo nella colonna comandi della mappa (`map-nearby-caravans-button`, badge rosso col numero) → lista dei convogli di altri giocatori (non alleati) in viaggio entro il raggio, ordinati per distanza, con casata, posizione, destinazione, scorta/carico stimati, ETA, stima **Raggiungibile/Fuori portata** (cavalleria 3 caselle/h vs ETA), «Mostra sulla mappa» (deep link `/map?fx&fy&ft` → `engine.centerOn`) e **«Saccheggia»** → compositore intercettazione esistente (`/caravan/intercept`). Raggio: `caravans.search_radius(research, world)` = max(Bibbia 5–12, `world.caravan_search_radius`) — impostato **50** su Regno 1 e 2 e default (`worlds.CARAVAN_SEARCH_RADIUS = 50`) per i nuovi Regni, su richiesta dell'utente. `search` ora ritorna `distance` e `target_xy`, ordinato per distanza. L'intercettazione resta fisica: `INTERCEPT_UNREACHABLE` se nessun punto della rotta è raggiungibile prima della consegna (tradotto nel toast).


### GRANDE MONDO — Fase 1 (Bibbia GM v0.2) — iteration 25 — testing agent backend 11/11 + frontend OK
- Architettura "Corona dei Regni" (SUPERATA da iteration 27, layout a spicchi): UN solo mondo (`kind: GRANDE_MONDO`, `gm_1`, era 3232×3232) con 9 regioni 600×600 (IT FR ES DE AT RU CN + bonus UK, PT; ognuna 100 slot + 800 neutrali, Piramide regionale al centro, distanze 16/20 e 7/9) su un anello attorno alla terra centrale neutrale con la Grande Piramide (1616,1616); ogni regione ha una "Via della Piramide" (istmo) con passo di montagna di compensazione → costo di percorso identico dalla costa regionale alla Grande Piramide.
- Muro di nebbia = regola server (`grande_mondo.check_target` + maschera A* `allowed`): in ISOLAMENTO nessuna marcia/carovana/intercettazione/piramide fuori dalla zona della regione (409 FOG_WALL); chunk fuori dalle zone visibili → solo terreno (`fogged: true`); overview senza castelli stranieri; ricerca carovane filtrata per zona.
- Ciclo persistente `worlds.gm` {phase, cycle, phase_since, phase_until, deadline_key}: ISOLATION 120g → WAR 20g → ISOLATION (ciclo+1) via evento scheduler `GM_PHASE_DEADLINE`; a ogni transizione avviso mondiale in inbox (`GRANDE_MONDO_PHASE`) + cronaca (GM_FOG_FALLEN / GM_FOG_RETURNED). QA: `POST /api/qa/grande-mondo/phase {world_id, to}`.
- Registrazione Mondo → Regione: `POST /worlds/{w}/join {house_name, region_code}` (REGION_REQUIRED / REGION_FULL, cap 100 per regione, `regions.$.player_count`); `player.region_code`, `settlement.region_code`.
- API: `world_dto.grande_mondo` (fase, countdown, regioni con posti liberi, centro, my_region), `GET /worlds/{w}/grande-mondo`, `POST /worlds` admin `{kind:"GRANDE_MONDO"}` (generazione in background) + `scripts/create_grande_mondo.py [--force]` (13 s).
- Prestazioni: A* con euristica ottile (900 tile 5 s → 0.2 s) ed eseguito fuori dall'event loop (`astar_async`); overview factor 8 per mondi > 1024; tile far-LOD costruiti solo in una finestra attorno alla camera; minimap sui bounds della regione.
- UI: card GRANDE MONDO in "Scegli il tuo Regno" (fase · countdown · bandiera regione) + sheet scelta regione (bandiera, lingua, posti liberi, piene disabilitate) → nome Casa; mappa: chip `Nebbia/Guerra · countdown · 🇮🇹 IT` → schermata /grande-mondo (countdown, barra, regole, 9 regioni, "Mostra sulla mappa"); muro di nebbia 3D (coltre + cortina animate, shader) sul confine regionale, camera confinata alla regione finché la nebbia è alta; inbox evento Grande Mondo.
- Piramide regionale: FATTO in iteration 28 (Piccole Piramidi con monumento 3D e ciclo completo).

#### Numeri da approvare (Bibbia GM: Emergent propone, Manuel decide)
- Distanze: dalla costa regionale alla Grande Piramide ~970 tile equivalenti (compensati); da un castello interno ~1200-1700 di costo → con Fanteria 2.4 t/h = 20-30 giorni, Cavalleria 4.8 = 10-15 giorni, Drago 18 = 3-4 giorni. Con una Guerra di 20 giorni e presidio di 168 h la Grande Piramide è raggiungibile solo da eserciti veloci. Opzioni: (a) bonus velocità ×3 sulle Vie della Piramide e sulla terra centrale durante la Guerra; (b) Guerra più lunga (30-40 g); (c) lasciare così (spedizione riservata a cavalleria/leggendarie).
- Premio Grande Piramide: FATTO in iteration 28 (7 gg +10% prod, +5% ricerca, +10% addestramento a tutta la regione vincitrice; titolo «Custode della Grande Piramide»).

### Prossime fasi Grande Mondo
- Fase 2 Piramidi: FATTA in iteration 28 (Grande Piramide ad apertura admin con fine anticipata della guerra + Piccole Piramidi). Resta aperto: timer 168 h che NON si azzera tra Alleanze della stessa regione (oggi il cambio di Alleanza azzera il timer, come nel ciclo classico).
- Fase 3 (FATTA, iter. 26): Teletrasporto castello.
- Fase 4 (FATTA, iter. 26): sei lingue + selettore.


### Sei lingue + Teletrasporto castello + Guerra 30 gg + Osservatore — iteration 26 — testing agent backend 12/12 + frontend OK
- **Lingue (Bibbia GM)**: it, en, fr, es, de, ru, zh (zh-CN), pt. Dizionari generati da `backend/scripts/translate_i18n.py` (LLM gpt-5.4-mini via Emergent key, cache `scripts/i18n_cache/`, rieseguibile con `--only-missing` dopo nuove chiavi) in `frontend/src/i18n/locales/<lang>.ts` (chiavi UI + errori API + risorse). `LANGS`, `normalizeLang`, `localeOf`, fallback inglese; selettori con bandiera nel login e in Impostazioni; la scelta della regione propone la sua lingua (checkbox nel foglio di adesione), il giocatore può cambiarla quando vuole senza cambiare regione.
- **Teletrasporto (Bibbia GM)**: `GET/POST /worlds/{w}/settlements/{s}/teleport` — castello conquistato (mai il Castello Madre) ↔ castello vuoto (slot FREE) della propria regione, 2000 Rubini fissi (ledger `TELEPORT_CASTLE`, idempotente per key), atomico (claim slot → debito → swap), solo slot con accesso al mare se il castello ha un Porto, rifiutato con marce in volo (`TELEPORT_BUSY`). Seguono il castello: territorio base, Sentinelle ri-ancorate (slot in acqua → rimossa, presidio torna in città), confini naturali; la riserva dello slot vuoto va alle vecchie coordinate. Registro `teleport_log`, cronaca e inbox `CASTLE_TELEPORTED`. UI: pulsante ⇄ nella scheda Città (solo GM, castelli non Madre) → `/teleport` con candidati (distanza dal Madre, terreno, ⚓) e foglio di conferma.
- **Guerra delle Regioni = 30 giorni** (decisione Manuel): `grande_mondo.DEFAULTS.war_days=30` + `gm_1.gm_config.war_days=30`.
- **Account Osservatore** (`scripts/observer_account.py`): `osservatore@empirelords.com / Demo12345!` — gm_1, regione IT, `view_all_regions` (vede tutte le regioni e la Grande Piramide senza nebbia; camera libera; le regole di movimento restano), home L30 + 2 villaggi, 999.999 Rubini.
- Nota: dopo un teletrasporto lo slot vuoto (stesso id) si trova alle vecchie coordinate e resta FREE: ri-sceglierlo riporta il castello indietro (altri 2000 Rubini) — comportamento voluto.

### Grande Mondo a spicchi + guerre selettive + switcher villaggi — iteration 27 — testing agent backend 25/25 + frontend OK
- **Layout a spicchi (pie)** (`worldgen.py`/`grande_mondo.py`): mondo **2176×2176**, centro (1088,1088), disco centrale r=130 con **Grande Piramide 41×41** (monumento 3D scalato ×2,7), 9 settori di 40° con terraferma da r=130 a r=880 e isole fino a r=1030, canale di mare 24 tile tra settori; regione DTO: `index, mid_deg, half_deg, r_in, r_land, r_out, bbox, center(=pyramid_anchor)`. Zone: 0 = centro, k = regione k-1 (`zone_grid`, `sector_index`; specchio frontend `zoneAt/allowedZones/zonesBounds/fogZonesFor` in `src/game/grandeMondo.ts`). Muro di nebbia 3D a settori (`fog.ts`).
- **Guerra selettiva** (decisione admin volta per volta): `POST /worlds/{w}/grande-mondo/admin/war-config {regions: [codici]|null, speed_multiplier ∈ {1,2,3,5}}` (400 WAR_CONFIG_INVALID con <2 regioni o moltiplicatore fuori lista; 403 se non `players.gm_admin`), `POST .../admin/phase {to}` (transizione reale). In GUERRA solo le regioni ammesse (+ centro) sono raggiungibili tra loro; le altre restano isolate (`my_fog_up` = la mia zona è l'unica raggiungibile). Le marce che escono dalla propria zona viaggiano al moltiplicatore (`speed_multiplier` nel preview/DTO, `pathfinding` con maschera `allowed`). gm_1: `next_war = FR+IT ×5`. Pannello admin in `/grande-mondo` (chip regioni + velocità, «Salva», «Fai cadere/alzare la nebbia» con conferma).
- **Switcher villaggi mappa** (`src/components/SettlementSwitcher.tsx`): chip scorrevoli + frecce ‹ › (villaggio precedente/successivo, la camera si centra sul villaggio scelto) + pulsante lista → foglio con tutti i villaggi (★ Madre, livello, coordinate). Minimappa spostata in basso a sinistra (i pulsanti +/- non la toccano più). Freccia indietro in `/grande-mondo`, `/settings`, `/teleport`.
- (Risolto in iteration 28) La Piramide **regionale** ora ha monumento 3D e gameplay — vedi sotto.

### Piccole Piramidi + Grande Piramide — iteration 28 — pytest 10/10 (+ classico 8/8) + testing agent frontend GREEN
- **Architettura** (`pyramid.py` multi-istanza): un documento `pyramid` per Piramide. Regno classico: `_id = world_id` (CLASSIC). Grande Mondo: `_id = world_id` = **Grande Piramide** (GRAND, 41×41 al centro) + `_id = "<world>:<CODE>"` = **Piccola Piramide** (REGIONAL, 15×15, ancora `regions[k].pyramid_anchor`). `config(world, pid)` = spec ⊕ default del kind ⊕ override (`worlds.pyramid_config` per CLASSIC/GRAND; `worlds.pyramid_regional_config["*"|CODE]` per le Piccole, salvare "tutte" cancella gli override per-regione delle stesse chiavi).
- **Piccola Piramide** (decisioni utente): regole classiche §21, **Guardiano fisso 250.000** (`guardian.min_power 250000, median_multiplier 0`), prima apertura **90 gg** dall'apertura del mondo (admin: tutte le regioni o una sola), **contendibile solo dalla propria regione** anche in Guerra (`409 PYRAMID_WRONG_REGION`; a destinazione → TARGET_INVALID), notifiche solo ai giocatori della regione.
- **Grande Piramide** (decisioni utente): `manual_open` — nessun calendario: l'admin la **apre solo durante la Guerra** (`POST …/grande-mondo/admin/grand-pyramid {action: OPEN|CLOSE}`, 409 GRAND_PYRAMID_NEEDS_WAR), si **chiude da sola quando torna la nebbia** (presidio a casa). Guardiano, Alleanze Strutturate, 168h → **7 gg di premio (+10% produzione, +5% ricerca, +10% addestramento) a TUTTI i giocatori della regione dell'Alleanza vincitrice** (slot `grand_pyramid_reward`, si somma al premio della Piccola), titolo «Custode della Grande Piramide», prestigio 500, Smeraldi all'Alleanza; alla vittoria **la guerra finisce subito** (transition ISOLATION, reason GRAND_PYRAMID_WON) e `gm.grand_wins` registra i Custodi.
- **API**: `GET /worlds/{w}/pyramid?id=`, `GET /worlds/{w}/pyramids` (lista compatta per la mappa), `MarchIn.pyramid_id`, `GET /grande-mondo` → `pyramids/grand_pyramid/my_pyramid/regional_first_open_day/regional_overrides/grand_wins`, `POST …/admin/regional-pyramid-config {region|null, config}`; QA `/qa/pyramid/config|reset` con `pyramid_id`/`all_regions`.
- **Frontend**: monumenti multipli nel motore (`engine.setPyramids`, picking/etichette/minimappa per ognuno, nascosti oltre la nebbia), card mappa per la Piramide toccata, `/pyramid?id=` con chips Grande/Piccola e premi per tipo, pannello «Le Piramidi» + Custodi + regola 4 in `/grande-mondo`, admin: Apri/Chiudi la Grande Piramide, prima apertura Piccole (tutte/una regione). 25 nuove chiavi i18n × 8 lingue.
- Test: `tests/test_iteration_28_pyramids.py` (ciclo Piccola IT con Guardiano 250k, WRONG_REGION, Grande: needs-war, apertura, cattura ×5, premio regionale, fine anticipata della guerra, chiusura al ritorno della nebbia, ripristino).


### Restyling grafico — iterations 29–30 — testing agent 12/14 → fix applicati e verificati (screenshot)
- Restyling **solo frontend** (vincolo utente): luce/ciclo giorno-notte con «ora blu» leggibile, città a tier (palizzate, lanterne, stendardi), Metropoli L30 (cinta esterna + bastioni + guglia dorata), `ResearchTree`, card Missioni/Edifici, percorsi marce (`marchPath.ts`). Nessun file backend di gioco toccato.
- **Fix post-test (iteration 30)**: `engine.tap()` — `labelHit()`: il tocco su un **chip etichetta** (marcia o targa castello, stesso clamp di `MapLabels.clampX`) seleziona ciò che il chip nomina invece del terreno/castello dietro (`engine.labelsShown` specchia il toggle etichette). Verificato: etichetta marcia → MarchCard con «Richiama»; etichette castello demo/lord → card giusta; etichetta Piccola Piramide → card Piramide. `daily-close`/`daily-done` → `canGoBack() ? back() : replace('/(tabs)/map')`. `textShadow*`/`shadow*` deprecati su web → helper `theme.textShadow()` (web shorthand CSS, native props classiche) + `boxShadow` per le braci della cinematica.
- **Traduzioni**: `translate_i18n.py --only-missing` ora rifà anche le chiavi «stale» (uguali all'IT mentre l'EN differisce) → RU/ES/PT ripuliti (~60 chiavi RU erano rimaste in italiano). Nomi/effetti ricerche e nomi unità/edifici restano dati canonici in italiano (spec server), per scelta.
- `missions-active-panel` era già presente nello stato vuoto (falso negativo del tester).

### Inattività 3/30/120, Cap Leggendari, Specializzazione in Casata, Guida primo accesso — iteration 31 — pytest 5/5 + testing agent frontend 5/5
- **Inattività** (decisioni utente): attività = qualsiasi richiesta autenticata al Regno (`players.last_active_at`, scrittura ogni ≥10 min in `worlds.get_player`). Sweep `INACTIVITY_SWEEP` per mondo ogni 6 h (`domain/inactivity.py`, bootstrap in `server.py`; i giocatori esistenti partono da `last_active_at = now`). **Regno < 30 gg**: 3 giorni consecutivi senza accesso → **rimozione**: slot Madre torna `PLAYER_SLOT FREE` (con raggio di riserva 4), ex-neutrali conquistati → NEUTRAL, `player_count` −1 (e regione −1 nel GM) ⇒ il posto è di nuovo disponibile per un nuovo giocatore. **Regno ≥ 30 gg**: 120 giorni → conversione a **Neutrale** (spec.neutral_conversion: livelli/edifici/HP mura conservati — `catch_up_neutral` rispetta `converted_from_player` —, risorse 50 %, guarnigione 100·L², territorio e Sentinelle rimossi, ricerca congelata), lo slot non riapre. In entrambi i casi: marce DISBANDED, code CANCELLED, uscita dall'alleanza, Casata `ELIMINATED` (`eliminated_reason` INACTIVE_EARLY|INACTIVE), audit + Cronaca `PLAYER_INACTIVE`. Esenti: fixture QA (`inactivity_exempt`: demo/lord/max/osservatore, impostato dai seed script) e Osservatore (`view_all_regions`). Override per mondo `worlds.inactivity_config` (solo QA). QA: `PUT /qa/inactivity/config`, `POST /qa/inactivity/touch {player_id, days_ago, exempt?}`, `POST /qa/inactivity/sweep`. DTO: `world.inactivity {phase EARLY|MATURE, timeout_days, mode REMOVE|NEUTRAL, early_phase_until, early_phase_days, early_timeout_days, timeout_days_after}`, `player.last_active_at`. UI: pannello «Regola inattività» in Impostazioni.
- **Cap Leggendari** (Bibbia §10, spec.legendary_stacking.metropolis_cap): max **3 per tipo per Metropoli** = guarnigione + coda residua + in marcia dall'origine (`recruitment.legendary_usage`); `409 LEGENDARY_CAP_REACHED`; `GET …/army` → `units[].legendary_cap`, `batch_cap = min(1, free)`, stato `BLOCKED_CAP`. UI Esercito: box «Cap Metropoli ●●● n/3 · Guarnigione · in coda · in marcia», pill «Cap raggiunto», foglio recluta con cap e pulsante disabilitato. Gestione UNHOUSED (perdita Metropoli) rinviata per scelta utente.
- **Specializzazione in Casata**: `SpecializationCard` (bonus attivo +5 % ATK/DEF con descrizione, cooldown, «disponibile ora» / «richiede 3 insediamenti (n/3)», pulsante Gestisci/Scegli → `/specialization`). Backend già esistente (iteration 12).
- **Guida primo accesso**: store `src/state/tour.ts` + `TourOverlay`/`TourGate`; 4 passi **Mappa → Città → Missioni → Piramidi** con spotlight (tab misurate da `MeasuredTabBar`, pulsante piramide registrato con `useTourTarget`), card storybook, «Salta la guida» / «Avanti» / «Inizia a giocare». Ordine dei gate di prima sessione: intro → tour → forziere giornaliero (`DailyGate` attende `tour_seen`). Vista una sola volta per Casata (`players.tour_seen_at`, `POST /worlds/{w}/tour/seen`; giocatori esistenti backfillati), replay da Impostazioni → «Rivedi la guida». Su iOS 26 (NativeTabs) la card è centrata senza spotlight sulle tab.
- i18n: 29 nuove chiavi + 2 errori API × 8 lingue (`translate_i18n.py`).
- Test: `tests/test_iteration_31_inactivity.py` (regola esposta, REMOVE con slot riaperto e contatore, NEUTRAL con livello conservato, esenzione fixture, cap Leggendari 3/3 → 409).

### Avviso inattività, Santuario Mitico + Unicorno (Bibbia §12), Guida contestuale — iteration 32 — pytest 3/3 (+31: 5/5) + testing agent frontend OK
- **Avviso inattività**: `inactivity.sweep_world` invia `INACTIVITY_WARNING` (Inbox, severity CRITICAL, deep link Impostazioni) ai giocatori a ≤ 1 giorno dall'eliminazione, una sola volta per periodo di inattività (dedupe `inactivity_warn:<pid>:<last_active_ts>`); testo Inbox «Accedi entro il {date}: dopo {n} giorni senza accesso la tua Casata viene eliminata e il posto torna libero / i castelli diventano neutrali».
- **Santuario Mitico** (`domain/mythic.py`, spec.mythic): potere player-wide (`players.sanctuary.level` 0..5), mostrato e migliorato **solo nella Madre** (riga del catalogo edifici con tabella dedicata costo/tempo in giorni, stati `AVAILABLE/BLOCKED_*/IN_PROGRESS/MAXED/LOCKED`, `MOTHER_ONLY` nei castelli secondari). Requisiti spec.unlock_registry: Metropoli L30 + «Studi Mitici». L'upgrade è un job `BUILDING` `player_wide` nella coda della Madre: Rubini vietati (`FORBIDDEN_MYTHIC`), non cancellato alla perdita della Madre (continua con snapshot), effetto applicato con `$max`. UI: `MythicPanel` in `/building/Santuario Mitico` (5 livelli con effetti/costi/tempi, pannello Unicorno).
- **Unicorno**: `POST /worlds/{w}/unicorn/summon` (Santuario L5, costo pagato dalla Madre, 7 gg fissi, 1 rituale per Casata, stati `NONE→QUEUED→READY→IN_FLIGHT→COOLDOWN 720 h`; errori `SANCTUARY_REQUIRED/UNICORN_ACTIVE/UNICORN_COOLDOWN`); `GET /worlds/{w}/mythic`; `player.unicorn` in `/me`; notifica `MYTHIC_RITUAL` (avviato/pronto).
- **Ponte Arcobaleno**: missione `RAINBOW_BRIDGE` in `marches.launch/preview` — solo bersaglio **PLAYER nemico** (mai neutrali/piramidi/sentinelle/navale), cap Sala di Guerra come ATTACK, ≤1 Leggendario, path rettilineo a 2 punti senza pathfinding (ignora distanza/acqua/montagne/Sentinelle), evento fisso 10 s, prenotazione cap20, Unicorno `READY→IN_FLIGHT`, **alert immediato** al difensore (`HOSTILE_MARCH_DETECTED` senza timer intel). All'arrivo: rivalidazione (PLAYER, diplomazia, prenotazione) → se fallisce ritorno `BRIDGE_CANCELLED` con Unicorno di nuovo READY (non consumato); altrimenti `mythic.consume` (COOLDOWN 720 h) + battaglia con regole ATTACK + vittoria → `conquest.transfer_ownership` **immediato** (retention 85 %, successione Madre, cap20). `_abort_side_effects` rende idempotente il rilascio di prenotazione e Unicorno su ritorno/richiamo. UI: chip «Ponte Arcobaleno» nel compositore solo con Unicorno READY e bersaglio PLAYER nemico; preview «evento di 10 s · nessun percorso»; sulla mappa la marcia è un **arco viola** (`marchPath.rainbowArc`). Rinviato: UNHOUSED Leggendari (già in backlog).
- **Guida contestuale**: `Hint` (una-tantum, `players.hints_seen` via `POST /worlds/{w}/hints/{key}/seen`) su Ricerca, Alleanza, Marce (lista + compositore, stessa chiave).
- QA: `POST /qa/mythic {player_id, sanctuary_level?, unicorn_state NONE|READY}`; `PUT /qa/inactivity/config` ora **richiede `only_player_ids`** (lo sweep con soglia accorciata è confinato ai giocatori indicati). ⚠️ Incidente QA: un test con soglia 3 gg senza scope aveva convertito 61 giocatori di prova di world_1 in neutrali; ripristinati da audit (`restored_reason: QA_SWEEP_ROLLBACK`: proprietà, status, territorio; eserciti/alleanze non recuperabili). Stato fixture: lord ha Santuario L5 e Unicorno **READY** (fixture QA) su Regno 2 per provare il Ponte.
- Test: `tests/test_iteration_32_mythic.py` (catalogo/upgrade 4→5, rituale → READY → bridge 10 s → conquista immediata + COOLDOWN, warning Inbox).

### Cinematica Arcobaleno, Skin marce premio Prestigio, «Riepilogo rientro» — iteration 33 — pytest 6/6 + testing agent frontend 4/4 flussi OK
- **Cinematica Arcobaleno** (§12.2 / §41.2): `RainbowWatcher` (montato in `(tabs)/_layout`) è attivo solo con Unicorno IN_FLIGHT/COOLDOWN, polla `/battles` ogni 4 s e riproduce una sola volta la cinematica CONQUEST `rainbow:true` (asset `assets/cinematics/rainbow_*.jpg`, lista SEEN condivisa con il rapporto battaglia). Nessun polling con Unicorno inattivo (verificato).
- **Skin marce** (§41.3): `house.SKIN_PRESTIGE` falcon 500 / elephant 2000 / dragon 5000; sblocco = possesso creatura OPPURE Prestigio ≥ soglia. `award_prestige` → `house.prestige_skin_unlocks` → Inbox `MARCH_SKIN_UNLOCKED` (dedupe per skin). PUT `/house` con skin bloccata → 409 `MARCH_SKIN_LOCKED`. Casata: card skin con requisito «Prestigio N (attuale) · oppure possiedi <unità>» / «Sbloccata», bloccate solo anteprima.
- **Riepilogo rientro**: `players.return_since/return_pending` armati da `inactivity.touch` quando due sessioni distano ≥ 6 h (`RETURN_GAP_H`). `GET /worlds/{w}/return-summary` (battaglie/castelli, code finite, marce rientrate, missioni, produzione stimata capped magazzino, allerte, non letti); `POST …/return-summary/seen`. `ReturnGate` apre `/return-summary` una volta per sessione dopo intro+tour e prima del Login giornaliero (`DailyGate` attende `return_pending=false`).
- QA: `POST /qa/return-summary/arm {player_id, hours_ago}`, `POST /qa/prestige {player_id, points}` (nuovo, path canonico con premio Inbox).
- Test: `backend/tests/test_iteration_33_return_skins.py` 6/6; testing agent frontend: flusso rientro (auto-apertura, chiusura, non ricompare al reload), skin sbloccate (demo) e bloccate → +600 prestigio → Falco sbloccata + Inbox, nessun errore JS.

### Negozio — iteration 34 (richiesta utente) — pytest 4/4 + testing agent frontend 5/5
- **Decisione owner (giugno 2026)**: supera la clausola «store DISABLED / i Rubini non comprano risorse» della Bibbia §23: i pacchetti in € possono includere unità (200 Orsi) e bonus.
- **Pacchetti Rubini (Google Play via RevenueCat, ID prodotto da creare uguali in Play Console)**: `eld_rubies_199` 1.99 € → 200 R + 1 skin castello tier 300 (prima non posseduta Ghiaccio→Foresta→Oceano; tutte possedute → 300 R) · `eld_rubies_499` 4.99 € → 500 R + 200 Orsi (premio account-level, ritirato nella Madre del Regno scelto) · `eld_rubies_999` 9.99 € → 1100 R · `eld_rubies_1999` 19.99 € → 2400 R · `eld_rubies_4999` 49.99 € → 6500 R, **×10 al primo acquisto** (65.000).
- **Skin castello premium (Rubini, account-level, valgono in ogni Regno)**: 300 Ghiaccio ❄️ / Foresta Elfica 🌲 / Oceano 🌊 · 600 Luce ✨ / Sole d'Oro Imperiale 👑 / Notte Stellata 🌙 · 900 Castello del Drago 🐉 / Castello del Demone 😈 / Vulcano 🌋. 3D: palette + tetti + torce colorate (glow) + aura a terra + ornamenti (corna, cristalli fluttuanti, disco solare, chiome).
- **Flusso pagamenti**: client `react-native-purchases` (appUserID = account_id) → Google valida → RevenueCat webhook `POST /api/webhooks/revenuecat` (Bearer `RC_WEBHOOK_AUTH`) → grant server idempotente per transaction_id → Inbox STORE_PURCHASE. Nessun accredito lato client. Finché `EXPO_PUBLIC_RC_ANDROID_KEY` è vuota / `STORE_BILLING_LIVE=false` la UI mostra «Google Play in arrivo» (sheet informativo, nessun addebito).
- **Da fare quando l'account Google Play è pronto**: creare i 5 prodotti one-time (consumabili) con gli ID sopra; progetto RevenueCat → chiave pubblica Android in `frontend/.env` `EXPO_PUBLIC_RC_ANDROID_KEY`; webhook RevenueCat → URL `https://<dominio>/api/webhooks/revenuecat`, header `Authorization: Bearer <RC_WEBHOOK_AUTH>`; `STORE_ENVIRONMENT=PRODUCTION` e `STORE_BILLING_LIVE=true` in `backend/.env`; build Android nativa (Expo Go non supporta gli acquisti).
- API: `GET /store`, `POST /store/skins/{id}/buy`, `POST /worlds/{w}/store/claim`, `POST /webhooks/revenuecat`; QA `POST /qa/store/purchase {product_id, email|account_id}`. UI: `/store` (pill rubini in Città, Portafoglio, /skins), `/skins` con skin premium.

### Fixture «Sovrano» nel Grande Mondo + orologio di gioco = ora italiana — iteration 35 (richiesta utente)
- **Decisione prodotto (utente, 13/09/2026)**: d'ora in avanti ogni mondo giocabile è un **Grande Mondo** (9 regioni 600×600 a spicchi). Regno 1, Regno 2 e il «Regno 3» 400×400 creato per errore sono stati **eliminati** (`scripts/reset_worlds.py`, `worlds.delete_world`); `POST /worlds` crea Grande Mondo di default (i classici solo `hidden` per la QA); `ensure_default_world` genera un Grande Mondo al primo avvio; `GET /worlds` nasconde i mondi `hidden`; nuovo `GET /worlds/{id}`.
- Script `backend/scripts/sovrano_account.py` (idempotente) — **gm_1 regione IT**:
  - `sovrano@empirelords.com / Sovrano12345!` — Casata «Casa Sovrana», madre (913,142): 20 castelli L30 (edifici/ricerche/esercito/mura al cap, skin castello tutte diverse), 240 sentinelle GUARDED (tutti gli slot su terra), prestigio 60k, Rubini 999.999, 9 skin castello + 4 skin marcia, Santuario L5 + Unicorno pronto, intro/tour già visti, esente inattività, scudo PvP chiuso, **niente nebbia** (`view_all_regions`) + comandi admin GM (`gm_admin`); le marce restano nella regione finché la Nebbia è alta.
  - `predone@empirelords.com / Predone12345!` — Casata «Casa Predone», vicino ostile a ~30 tile (madre L20 + 1 villaggio).
  - Traffico vivo: 6 marce del Sovrano (ATTACK drago ×2, RAID elefante, ATTACK falco, REINFORCE classica), 4 carovane in arrivo tra castelli propri, 2 carovane del Predone **intercettate** dal Sovrano (INTERCEPT «saccheggio»), 1 INTERCEPT del Predone su una carovana del Sovrano, 1 RAID del Predone verso un castello del Sovrano (compare in «in arrivo» solo dopo il rilevamento intel, ETA ~8 h).
- **Orologio**: l'offset QA accumulato dai test (+237 giorni) è stato azzerato con `POST /api/qa/clock/reset` (`app/core/clock_shift.py`: sposta ogni datetime del DB di −offset e persiste 0). ⚠️ Lezione (13/09): la suite e2e lanciata contro il backend live ha avanzato il Grande Mondo di 99 giorni, fatto arrivare tutte le marce e **eliminato per inattività il player dell'utente (TiDemolisco)** — ripristinato a mano (`audit.player_restored`). Da allora la suite gira SOLO sul **backend QA** `:8002` con DB separato `eld_qa` (`scripts/qa_backend.sh`, `scripts/seed_qa_db.py`, `scripts/e2e.sh`; `tests/e2e_base.py` rifiuta gli host live). Il reset dell'orologio va usato solo se l'offset live è ≠ 0 e SOLO se tutti i dati sono stati scritti sotto quell'offset (altrimenti sposta anche dati «reali»).
- **Ora italiana**: orologio del Regno (mappa/luce del giorno) ora `Europe/Rome` con ora legale (`daylight.ts romeOffsetHours`); giorno del Login giornaliero calcolato in `Europe/Rome` (`daily.py REALM_TZ`). Server in UTC; il client mostra l'ora locale.
